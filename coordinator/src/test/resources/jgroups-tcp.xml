<!--
  JGroups TCP configuration for unit testing.

  Uses TCP instead of multicast for better compatibility in test environments.
  Uses TCPPING with localhost for discovery.
-->
<config xmlns="urn:org:jgroups"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="urn:org:jgroups http://www.jgroups.org/schema/jgroups.xsd">

    <!-- TCP transport with dynamic port allocation -->
    <TCP bind_addr="127.0.0.1"
         bind_port="7800"
         port_range="50"
         recv_buf_size="20m"
         send_buf_size="640k"
         thread_naming_pattern="pl"
         thread_pool.enabled="true"
         thread_pool.min_threads="0"
         thread_pool.max_threads="25"
         thread_pool.keep_alive_time="5000" />

    <!-- Static discovery using localhost ports -->
    <TCPPING async_discovery="true"
             initial_hosts="127.0.0.1[7800],127.0.0.1[7801],127.0.0.1[7802],127.0.0.1[7803],127.0.0.1[7804]"
             port_range="5" />

    <MERGE3 min_interval="10000"
            max_interval="30000" />

    <FD_SOCK />

    <FD_ALL timeout="10000"
            interval="3000" />

    <VERIFY_SUSPECT timeout="1500" />

    <pbcast.NAKACK2 use_mcast_xmit="false"
                    discard_delivered_msgs="true"
                    log_discard_msgs="false"
                    log_not_found_msgs="false" />

    <UNICAST3 conn_close_timeout="5000" />

    <pbcast.STABLE stability_delay="500"
                   desired_avg_gossip="5000"
                   max_bytes="1M" />

    <pbcast.GMS print_local_addr="false"
                join_timeout="3000"
                leave_timeout="1000" />

    <MFC max_credits="2m"
         min_threshold="0.40" />

    <FRAG3 frag_size="8000" />
</config>
